#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <vector>
#include <stack>
#include <queue>
#include <string>
#include <map>
#include <set>
#include <algorithm>

using namespace std;

int main() {
    char ch[100];
    int a;
    printf("%9.g\n", 1.6789);
    return 0;

    scanf("%d%c", &a, ch);
    printf("a = %d, ch = %c\n", a, ch);
    getchar();

    scanf("%d %c", &a, ch);
    printf("a = %d, ch = %c\n", a, ch);
    getchar();
    scanf("%d%s", &a, ch);
    printf("a = %d, ch = %c\n", a, ch[0]);
    return 0;
}

