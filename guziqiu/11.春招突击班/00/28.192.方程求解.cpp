#include <iostream>
#include <cmath>
using namespace std;
#define MAX_N 

double func(double x, double n) {
    return x * exp(x) - n;
} 
double binary_search1(double a) {
    
   
    return -1;
}

//𝑥𝑒𝑥=𝑎
// xln(xe) = ln(a)
// xlnx + x = ln(a)
int main() {
    int a;//输入一个整数𝑎(0≤𝑎≤109)。
    cin >> a;


    return 0;
}
